{% extends 'base.html.twig' %}
{% form_theme form _self %}



{% block _registration_extraData_row %}
    {{ form_widget(form) }}
{% endblock %}



{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <button class="btn btn-primary">{{ 'submit'|tr }}</button>
        </div>
    {{ form_end(form) }}
{% endblock %}
