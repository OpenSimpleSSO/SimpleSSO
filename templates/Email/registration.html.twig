{% extends 'Email/baseEmail.html.twig' %}



{% block body %}
    <h1>{{ 'email.registration.greeting'|trans({ '%displayName%': userAccount.displayName }) }}</h1>

    <p>{{ 'email.registration.content'|trans }}</p>

    <p><a href="{{ url('userManagement.confirmEmailAddress', { token: userAccount.token }) }}">{{ 'email.registration.confirmEmailAddress'|trans }}</a></p>
{% endblock %}
